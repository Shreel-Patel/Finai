# Core API
fastapi
uvicorn
pandas
numpy
pydantic

# ML / prediction
scikit-learn
xgboost

# Data & HTTP
yfinance
requests

# Sentiment (pipeline)
tqdm
nltk
torch
transformers

# Phase 2: LLM agent (optional; chat falls back to intent flow if not installed)
ollama>=0.3.0
